replicaCount: 1

image:
  repository: tic-tac-toe-ynov
  pullPolicy: IfNotPresent
  # Pour des déploiements prévisibles, évitez "latest".
  # Laissez vide pour utiliser la valeur de .Chart.AppVersion.
  tag: ""

service:
  enabled: false
  port: 80 # Port fictif car l'app n'expose rien

ingress:
  enabled: false
  host: chart-example.local

config:
  PYTHONUNBUFFERED: "1"
  APP_ENV: "production"

secret:
  DUMMY_SECRET: "super_secret_value"

resources:
  limits:
    cpu: 200m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 64Mi